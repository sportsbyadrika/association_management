# ------------------------------------------------------------------
# Habitract environment configuration
# Copy this file to `.env` and fill in real values. NEVER commit .env.
# ------------------------------------------------------------------

APP_NAME=Habitract
APP_ENV=local                 # local | production
APP_DEBUG=true                # true only in local; false in production
APP_URL=http://localhost:8000
APP_KEY=                       # random 32+ char string; used for signing

# Database (MySQL 8 in production). DB_CONNECTION may be mysql | sqlite.
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=habitract
DB_USERNAME=habitract
DB_PASSWORD=habitract
# For DB_CONNECTION=sqlite, set the absolute path instead:
DB_SQLITE_PATH=storage/database.sqlite

# Session / security
SESSION_NAME=habitract_session
SESSION_IDLE_TIMEOUT=1800      # seconds of inactivity before logout (30 min)
SESSION_ABSOLUTE_TIMEOUT=28800 # absolute session lifetime (8 h)

# Mail (PHPMailer over SMTP) — used for password reset
MAIL_MAILER=smtp
MAIL_HOST=smtp.mailtrap.io
MAIL_PORT=2525
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_ENCRYPTION=tls           # tls | ssl | none
MAIL_FROM_ADDRESS=no-reply@habitract.local
MAIL_FROM_NAME=Habitract

# Seed super admin (created on first migration/seed run).
SUPER_ADMIN_NAME=Super Admin
SUPER_ADMIN_EMAIL=admin@habitract.local
SUPER_ADMIN_PASSWORD=ChangeMe!123

# Login throttling
LOGIN_MAX_ATTEMPTS=5
LOGIN_DECAY_MINUTES=15

# Password reset token lifetime (minutes)
RESET_TOKEN_TTL=60
